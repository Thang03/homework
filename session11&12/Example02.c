#include <stdio.h>

void main()
{
    int ary[10];
    int i, total, high;

    for (i = 0; i < 10; i++)
    {
        printf("Enter the value : %d\n", i + 1);
        scanf("%d", &ary[i]);
    }
    high = ary[0];
    for (i = 0; i < 10; i++);
}